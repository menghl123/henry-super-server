server:
  port: 8082

spring:
  application:
    name: henry-file
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

app:
  security:
    enabled: false
  file:
    storage-path: ./uploads
